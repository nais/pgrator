---
apiVersion: nais.io/v1
kind: PostgresBinding
metadata:
  name: reporter-mydb
  namespace: myteam
spec:
  postgres: mydb
  consumer:
    workload:
      name: reporter
      type: job
  secretName: mydb-reporter-read-client-cert
  role: read
