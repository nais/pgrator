---
apiVersion: nais.io/v1
kind: PostgresBinding
metadata:
  name: reporter-mydb
  namespace: myteam
spec:
  postgres: mydb
  secretName: reporter-mydb-connection
  consumer:
    workload:
      name: reporter
      type: job
  credentials:
    - read
