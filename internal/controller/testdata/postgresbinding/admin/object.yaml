---
apiVersion: nais.io/v1
kind: PostgresBinding
metadata:
  name: myapp-mydb
  namespace: myteam
spec:
  postgres: mydb
  secretName: myapp-mydb-connection
  consumer:
    workload:
      name: myapp
      type: application
  credentials:
    - admin
