---
action: exclusiveCreateOrUpdate
matcher: Subset
object:
  apiVersion: v1
  kind: Secret
  metadata:
    name: myapp-mydb-connection
    namespace: myteam
  stringData:
    # Admin binder seg mot den varige eier-rollen, ikke en rolle per workload.
    PGUSER: app
    PGHOST: pg-mydb-pooler.myteam
