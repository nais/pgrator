apiVersion: data.nais.io/v1
kind: Postgres
metadata:
  name: example
  namespace: nais
spec:
  cluster:
    allowDeletion: true
    majorVersion: "17"
    resources:
      diskSize: "2Gi"
      cpu: "100m"
      memory: "1Gi"
