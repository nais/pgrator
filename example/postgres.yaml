apiVersion: data.nais.io/v1
kind: Postgres
metadata:
  name: example
  namespace: platting
spec:
  cluster:
    audit:
      enabled: true
    allowDeletion: true
    majorVersion: "17"
    resources:
      diskSize: "2Gi"
      cpu: "100m"
      memory: "1Gi"
